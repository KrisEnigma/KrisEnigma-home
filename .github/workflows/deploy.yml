name: Legacy Repo Deploy Disabled

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy deploy disabled
        run: |
          echo "Legacy deploy to KrisEnigma/krisenigma.com is disabled."
          echo "Deploy this project directly from Cloudflare (Pages or Workers)."
